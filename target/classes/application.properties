# datasource
spring.datasource.url=jdbc:mysql://localhost:3306/project_management_system
spring.datasource.username=root
spring.datasource.password=admin
spring.jpa.database=mysql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#view
spring.thymeleaf.suffix=.html
spring.web.resources.static-locations=classpath:/static/

#validation
NotEmpty.User.name=Username must not be empty.
NotEmpty.User.email=User Email must not be empty.
NotEmpty.User.password=User Password must not be empty.
NotEmpty.User.cPassword=User Password Confirmation must not be empty.
NotEmpty.User.role=User Role must not be empty.

upload.path=/home/tinhtet/Documents/files

#Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=shunmay8999@gmail.com
spring.mail.password= hwou vmam vkja bveq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Maximum allowed size for uploaded files.
#spring.servlet.multipart.max-file-size=10MB

# Maximum allowed size for multipart/form-data requests.
#spring.servlet.multipart.max-request-size=10MB

# Enable support for multipart requests (file uploads).
spring.servlet.multipart.enabled=true

# Location to store temporarily uploaded files.
# You can specify a custom directory where uploaded files are stored temporarily.
# spring.servlet.multipart.location=/path/to/your/directory

# File extensions that are not allowed to be uploaded.
# You can customize this list based on your application's requirements.
#spring.servlet.multipart.fileExtensions=jpg,jpeg,gif,png

# Whether to resolve the multipart request lazily at the time of file or parameter access.
# By default, it's set to false, meaning the entire request is processed upfront.
#spring.servlet.multipart.resolve-lazily=false

# CORS configuration
#cors.allowed-origins=http://localhost:8080
#cors.allowed-methods=GET,POST,PUT,DELETE
#cors.allow-credentials=true
